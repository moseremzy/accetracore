<!DOCTYPE html>
<html lang="en">
<head>
  <link rel="stylesheet" href="/css/transaction.css">
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.6.1/jquery.min.js"></script>
    <script src="/js/interactive.js"></script>
    <meta name="google" content="notranslate">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&family=Lora:wght@400;700&display=swap" rel="stylesheet">
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <script type="text/javascript" src="//translate.google.com/translate_a/element.js?cb=googleTranslateElementInit"></script>
    <script src="https://kit.fontawesome.com/0855e51304.js" crossorigin="anonymous"></script>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="icon" href="/assets/img/favicon.png" type="image/x-icon" />
    <title>Transaction History</title>
    <%- settings.LIVECHAT %>
</head>
<body>

  <div class="container">

  <!--side bar--> <%- include('../partials/sidebar') %>

     <div class="sub_container">

      <!--header--> <%- include('../partials/header') %>


       <div class = "overall_grid">
         
         <h1>Transactions</h1>

         <div class = "transaction_container">
            <h2>Transaction History</h2>       
            <table cellspacing = "0" id = "latest_investment">
              <tr id = "table_row">
                <th>ID</th>
                <th>Type</th>
                <th>Date</th>
                <th>Amount</th>
                <th>Description</th>
              </tr>
              <% transactions.forEach((transaction) => { %>
                <tr>
                  <td>TNX<%= transaction.transaction_id %>_<%= transaction.user_id %></td>
                  
                  <td class="<%= transaction.kind.toLowerCase() %>"><%= transaction.kind %></td>
                  
                  <td><%= transaction.date %></td>
                  
                  <td style="color: <%= transaction.kind.toLowerCase() === 'debit' ? 'red' : 'green' %>;">
          
                    <%
                      const formatted_amount = new Intl.NumberFormat("en", {
                        style: "currency",
                        currency: user.currencyCode,
                        maximumSignificantDigits: 3
                      }).format(transaction.amount);
                    %>

                    <%= formatted_amount %>
                  </td>
                  
                  <td><%= transaction.description %></td>
                </tr>
              <% }) %>              
            </table>
        </div>

       </div>

       <!--footer--> <%- include('../partials/footer') %>

     </div>

    </div>

    <div id="google_translate_element" style="display:none"></div>
    <!-- define function BEFORE loading Google Translate -->
<script type="text/javascript">
  function googleTranslateElementInit() {
    new google.translate.TranslateElement({pageLanguage: "en"}, 'google_translate_element');
  }

  function changeLanguageByButtonClick() {
    var language = document.getElementById("language").value;
    var selectField = document.querySelector("#google_translate_element select");
    for (var i = 0; i < selectField.children.length; i++) {
      var option = selectField.children[i];
      if (option.value === language) {
        selectField.selectedIndex = i;
        selectField.dispatchEvent(new Event('change'));
        break;
      }
    }
  }
</script>

<!-- Now load the script -->
<script type="text/javascript" src="//translate.google.com/translate_a/element.js?cb=googleTranslateElementInit"></script>

  <script src="/js/helper.js"></script>
</body>
</html>